# .github/workflows/infra-pipeline.yml
# Pipeline de infraestructura como código con Terraform.
# Se dispara manualmente (workflow_dispatch) o automáticamente cuando cambia terraform/.
# El apply requiere aprobación manual en GitHub Environments para evitar cambios accidentales.
name: Infrastructure Pipeline

on:
  workflow_dispatch:  # Permite ejecución manual desde la UI de GitHub Actions
  push:
    branches:
      - main
    paths:
      - "terraform/**"  # Solo se dispara si hay cambios en Terraform

jobs:
  terraform-plan:
    name: Terraform Plan
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.0"

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          # La SA maestra (tf-deployer) tiene permisos para crear/modificar recursos GCP
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Terraform Init
        # Init descarga providers y configura el backend GCS remoto para el tfstate
        run: terraform init
        working-directory: terraform/environments/dev

      - name: Importar SA cloudrun-sa al estado de Terraform
        # Se usa terraform import en lugar de import block para mayor confiabilidad.
        # state rm previo garantiza estado limpio antes de importar (idempotente con || true).
        run: |
          terraform state rm module.platform.google_service_account.cloudrun_sa || true
          terraform import \
            -var="project_id=${{ secrets.GCP_PROJECT_ID }}" \
            -var="region=${{ secrets.GCP_REGION }}" \
            module.platform.google_service_account.cloudrun_sa \
            "projects/${{ secrets.GCP_PROJECT_ID }}/serviceAccounts/cloudrun-sa@${{ secrets.GCP_PROJECT_ID }}.iam.gserviceaccount.com"
        working-directory: terraform/environments/dev

      - name: Terraform Plan
        run: |
          terraform plan \
            -var="project_id=${{ secrets.GCP_PROJECT_ID }}" \
            -var="region=${{ secrets.GCP_REGION }}" \
            -out=tfplan
        working-directory: terraform/environments/dev

  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    needs: terraform-plan
    # Solo aplica en main y requiere aprobación manual via GitHub Environment 'production'
    # Configurar en GitHub: Settings → Environments → production → Required reviewers
    if: github.ref == 'refs/heads/main'
    environment: production

    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.0"

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Terraform Init
        run: terraform init
        working-directory: terraform/environments/dev

      - name: Importar SA cloudrun-sa al estado de Terraform
        # Mismo paso que en el plan: garantiza que el estado es consistente antes del apply.
        run: |
          terraform state rm module.platform.google_service_account.cloudrun_sa || true
          terraform import \
            -var="project_id=${{ secrets.GCP_PROJECT_ID }}" \
            -var="region=${{ secrets.GCP_REGION }}" \
            module.platform.google_service_account.cloudrun_sa \
            "projects/${{ secrets.GCP_PROJECT_ID }}/serviceAccounts/cloudrun-sa@${{ secrets.GCP_PROJECT_ID }}.iam.gserviceaccount.com"
        working-directory: terraform/environments/dev

      - name: Terraform Apply
        run: |
          terraform apply -auto-approve \
            -var="project_id=${{ secrets.GCP_PROJECT_ID }}" \
            -var="region=${{ secrets.GCP_REGION }}"
        working-directory: terraform/environments/dev
